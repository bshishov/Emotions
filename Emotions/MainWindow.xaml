<Window x:Class="Emotions.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Emotions"
        Title="MainWindow" Height="420" Width="787" Closed="WindowClosed">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Black">
            <Viewbox Stretch="Uniform">
                <local:KinectViewer x:Name="Viewer" Width="640" Height="480"/>
            </Viewbox>
        </Grid>
        <ListBox Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" ItemsSource="{Binding AUCoeffs}" Margin="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:RangedProperty Caption="{Binding Name}" Value="{Binding Value}" Min="-1" Max="1" DoubleSisded="True"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
